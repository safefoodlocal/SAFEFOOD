document.querySelector('.menu')?.addEventListener('click', () => { const links = document.querySelector('.links'); const open = links.classList.toggle('open'); document.querySelector('.menu').setAttribute('aria-expanded', open) }); document.querySelectorAll('form').forEach(form => form.addEventListener('submit', e => { e.preventDefault(); if (!form.checkValidity()) return form.reportValidity(); const note = form.querySelector('.message'); note.textContent = 'Thank you — connect this form to your CRM or email service before launch.'; note.style.display = 'block'; form.reset() }));
